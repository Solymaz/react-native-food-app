import React from "react";
import { View, Text, StyleSheet, Image } from "react-native";

export default function ResultsShowScreen({ result }) {
  return <Text>Results Show</Text>;
}

const styles = StyleSheet.create({});
